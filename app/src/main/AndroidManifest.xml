<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duongthuhien.kltn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".katakana.KatakanaActivity" />
        <activity android:name="GTCB.GTCBActivity" />
        <activity android:name="kanji.KanjiActivity" />
        <activity android:name="GTCB2.GTCB2Activity" />
        <activity android:name=".hiragana.HBCCHiraganaActivity" />
        <activity android:name=".hiragana.BCCHiraganaActivity" />
        <activity android:name=".hiragana.HiraganaActivity" />
        <activity android:name=".hiragana.ResultWordActivity" />
    </application>

</manifest>